import { User } from 'src/app/models/user.model';

export const environment = {
  production: true,
  ApiUrl: 'http://localhost:3000/',
  user: new User
  // ApiUrl: 'https://api-maui.herokuapp.com/',
};
